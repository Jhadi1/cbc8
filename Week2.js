let array = [brush, comb, mirror];
const removeElement = function(a, index) {
   let newArray = [...a];
   newArray.splice(index, 1);
    var str = document.getElementById("text1").value;
return newArray;
};

//Exercise countdown
